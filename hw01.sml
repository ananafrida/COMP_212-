val x : int = 12

fun assemble (x : int, y : real) : int =
let val q : real = let val x : int = 3
val p : real = 5.2 * (real x)
val y : real = p * y
val x : int = 123
in
print(Real.toString(p))
end
in
x + (trunc q)
end

val z = assemble (x, 2.0)